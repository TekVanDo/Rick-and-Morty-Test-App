import { EnumKeysPipe } from './enum-keys-pipe.pipe';

describe('EnumKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
