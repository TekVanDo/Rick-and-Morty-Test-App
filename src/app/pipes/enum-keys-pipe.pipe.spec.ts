import { EnumKeysPipePipe } from './enum-keys-pipe.pipe';

describe('EnumKeysPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EnumKeysPipePipe();
    expect(pipe).toBeTruthy();
  });
});
